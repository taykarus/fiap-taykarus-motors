spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/motors
    username: postgres
    password: mudar321
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
